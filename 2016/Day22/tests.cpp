#define CATCH_CONFIG_MAIN
#include "../lib/catch.hpp"

TEST_CASE("Magic fight","[magic fight]") {
    // There are no unit tests for this day because the implementation cost of these tests
    // is higher than the benefit in this specific test case
    REQUIRE (1==1);
}
